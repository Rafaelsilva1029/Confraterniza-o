{
  "version": 2,
  "buildCommand": "pnpm build",
  "devCommand": "pnpm dev",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": "dist",
  "env": {
    "DATABASE_URL": {
      "description": "URL de conexão do banco de dados Supabase (PostgreSQL)"
    },
    "NODE_ENV": {
      "description": "Ambiente de execução"
    }
  }
}
