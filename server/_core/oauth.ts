import type { Express } from "express";

/**
 * OAuth desativado - Aplicativo público sem autenticação
 * Todas as rotas de OAuth foram removidas
 */
export function registerOAuthRoutes(app: Express) {
  // OAuth routes disabled - application is public without authentication
}
